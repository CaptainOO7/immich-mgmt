[project]
name = "immich-mgmt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "egonever", email = "iron.junh@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
immich-mgmt = "immich_mgmt:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
